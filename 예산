def solution(d, budget):
    answer = 0
    d.sort()
    sum = 0
    for i in range(0,len(d)):
        sum= sum+d[i]
        if(sum>budget):
            break;
        answer=answer+1
    return answer
